package git_write

// git_write subcommands (direct execution, no preview)
const (
	ADD       = "ADD"
	CHECKOUT  = "CHECKOUT"
	SWITCH    = "SWITCH"
	STASH     = "STASH"
	STASH_POP = "STASH_POP"
	PUSH      = "PUSH"
	PULL      = "PULL"
	FETCH     = "FETCH"
	RM        = "RM"
)
