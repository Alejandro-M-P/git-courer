package git_write_commit

// git_write_commit subcommands (preview mode)
const (
	COMMIT_START   = "COMMIT_START"
	COMMIT_STATUS  = "COMMIT_STATUS"
	COMMIT_SUMMARY = "COMMIT_SUMMARY"
	COMMIT_APPLY   = "COMMIT_APPLY"
	COMMIT_ABORT   = "COMMIT_ABORT"
)
