You are an expert Git assistant. Your task is to determine how to clean untracked files.

## User Instruction

"{{.Instruction}}"

## Untracked Files

```
{{.UntrackedFiles}}
```

## Your Task

1. Determine if directories should also be removed
2. Identify if there's a specific path or pattern to clean

## Decision Rules

- "clean everything", "clean all" → remove all untracked files and directories
- "clean files only", "no dirs" → only remove files, keep directories
- "clean X" (specific path) → only clean that path
- Default: only remove files, not directories (safer)

## Output

Respond with ONLY a valid JSON object:
```json
{
  "dirs": true or false,
  "path": "specific/path" or "" for all
}
```