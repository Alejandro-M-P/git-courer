Analyze the following project directory structure and generate a project configuration.

Directory structure:
{{.DirectoryTree}}

Output ONLY a strict JSON object with this schema (no markdown fences, no extra text):
{
  "description": "A concise one-sentence description of what this project does",
  "areas": {
    "area_name": ["path/prefix/1", "path/prefix/2"]
  }
}

Rules for areas:
- Each area key is a short lowercase name (e.g. "auth", "core", "ui")
- Each path prefix should end with "/" and correspond to a real directory in the structure
- Group related directories under meaningful area names
- Include 3-8 areas that cover the main concerns of the project
- Path prefixes must be relative (no leading /)
- Prefer specific prefixes over broad ones (prefer "internal/auth/" over "internal/")
- A directory may appear in only ONE area (first-match wins)