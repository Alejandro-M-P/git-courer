JSON only.

{{if .Context}}{{.Context}}
{{end}}
Type: {{.CommitType}}{{if .Scope}}({{.Scope}}){{end}}{{if .Breaking}} ⚠BREAKING{{end}}

{{.AnnotatedDiff}}
{{if .RejectedMessage}}
Fix: {{.RejectedMessage}}
{{end}}
description = imperative, specific function, under 60 chars, no period
body = why (problem, symptom)

Example:
{"description": "add method guard in HandleRequest", "body": "Handler now rejects unsupported methods early instead of processing invalid requests."}

NO: improve, enhance, ensure, maintain, robust