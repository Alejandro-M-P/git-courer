Translate these commits into user-facing release notes, grouped by area.
{{if .Context}}Project: {{.Context}}

{{end}}{{.Groups}}

Rules:
- Translate each commit into clear, user-facing language. Never repeat raw commit text.
- BAD: "feat(core): add semantic annotator" → GOOD: "Added semantic diff analysis for more accurate commit detection"
- BAD: "fix(security): nil token" → GOOD: "Fixed a crash when the authentication token was missing"
- If a commit contains "!" it is a breaking change — explain what callers must update
- Keep each item to one sentence
- Output ONLY valid JSON where keys are the exact area names shown above and values are arrays of translated strings
- Only include areas that have user-facing changes
- Example: {"security": ["Fixed auth bypass when token was empty"], "core": ["Added support for semantic diffs"]}
