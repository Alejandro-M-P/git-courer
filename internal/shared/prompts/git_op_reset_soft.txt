Identify how many commits to undo (soft reset) from the following instruction.

Instruction: "{{.instruction}}"

Recent commits:
{{.recent_commits}}

RULES:
- Return the NUMBER of commits to undo as a string
- If the instruction says "last commit" → "1"
- If the instruction says "last 3 commits" → "3"
- If unclear, return "1"

Output ONLY valid JSON, no markdown:
{"subcommand": "1"}
