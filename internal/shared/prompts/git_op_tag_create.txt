Generate a git tag name from the following instruction.

Instruction: "{{.instruction}}"

Existing tags: {{.tags}}
Recent commits: {{.recent_commits}}

RULES:
- Use semantic versioning when possible: v1.0.0, v2.1.3
- If the instruction specifies a version, use it exactly
- If no version is specified, increment the patch version of the latest tag
- If no existing tags, start at v0.1.0

Output ONLY valid JSON, no markdown:
{"tag": "v1.0.0"}
