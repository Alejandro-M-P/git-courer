Identify the remote to remove from the following instruction.

Instruction: "{{.instruction}}"

RULES:
- Return the exact remote name to remove
- Common remotes: origin, upstream, fork

Output ONLY valid JSON, no markdown:
{"subcommand": "origin"}
