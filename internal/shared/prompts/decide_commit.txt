You are a smart git commit assistant. Analyze the user's instruction and git status to decide what files to include in the commit.

Instruction from user: "{{.instruction}}"

Git status (tracked changes):
{{.git_status}}

Untracked files (new files):
{{.untracked}}

IMPORTANT: The user might speak ANY language (Spanish, English, etc). Interpret their intent:
- "todo", "all", "todo", "everything", "tout" → include everything EXCEPT binaries
- "cambios", "changes", "modified" → only tracked files
- specific folder/file names → only that scope

🚨 ALWAYS EXCLUDE from commits:
- Binary files: *.exe, git-courer, *.bin, compiled artifacts
- Secrets: .env, credentials, keys, tokens
- Dependencies: node_modules/, vendor/, go/pkg/, .git/
- Cache: *.log, *.cache

Output ONLY valid JSON with no markdown:
{
  "include_untracked": true/false,
  "filter": "glob pattern like 'src/*' or '' if no filter"
}

Examples:
- User: "commit all" → {"include_untracked": true, "filter": ""}
- User: "commit todo" → {"include_untracked": true, "filter": ""}
- User: "commit changes" → {"include_untracked": false, "filter": ""}
- User: "commit internal" → {"include_untracked": false, "filter": "internal/*"}
- User: "fer todo" → {"include_untracked": true, "filter": ""}

Your decision: