Identify the branch to delete from the following instruction.

Instruction: "{{.instruction}}"

Current branch: {{.current_branch}}
Existing branches: {{.branches}}

RULES:
- Pick the branch that best matches the instruction
- NEVER select the current branch
- Return the exact branch name as it appears in the list

Output ONLY valid JSON, no markdown:
{"branch": "exact-branch-name"}
