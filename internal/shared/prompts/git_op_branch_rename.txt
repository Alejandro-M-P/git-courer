Identify the branch to rename and generate its new name from the following instruction.

Instruction: "{{.instruction}}"

Current branch: {{.current_branch}}
Existing branches: {{.branches}}

RULES:
- "branch" must be the exact current name of the branch to rename (from the list)
- "new_branch" must follow the same conventions: kebab-case, type prefix (feat/, fix/, etc.), under 40 chars
- If the instruction doesn't specify which branch to rename, assume the current branch

Output ONLY valid JSON, no markdown:
{"branch": "old-branch-name", "new_branch": "feat/new-name"}
