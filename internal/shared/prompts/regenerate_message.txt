The user rejected the previous commit message. Generate a NEW, BETTER, MORE DETAILED commit message.

Previous message (rejected): "{{.previous_message}}"

User feedback: "{{.feedback}}"

Files changed:
{{.files}}

Diff:
{{.diff}}

INSTRUCTIONS:
1. Address the user's feedback directly
2. If feedback is "too long" → keep detailed but remove filler words
3. If feedback is "not specific" → add specific details about WHAT and WHY
4. If feedback is "wrong type" → use the correct conventional type prefix
5. If feedback is about language → write in the same language as the feedback
6. NO character limit! Be generous with explanations
7. Keep good parts of the previous message if feedback didn't mention them
8. Focus on explaining the WHY, not just the WHAT

Output ONLY the new commit message with detailed explanation.
Start with type prefix: feat fix chore docs refactor test perf

New detailed commit message: