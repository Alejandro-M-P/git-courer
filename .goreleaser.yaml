version: 2

project_name: git-courer

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/main.go
    binary: git-courer
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/Alejandro-M-P/git-courer/internal/config.ServerVersion={{.Version}}
    flags:
      - -trimpath
    hooks:
      post:
        - upx --ultra-brute "{{ .Path }}"

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

release:
  prerelease: auto
  mode: replace
  header: |
    ## What's Changed

    {{ .TagBody }}
  footer: |
    ---

    **Full Changelog**: https://github.com/Alejandro-M-P/git-courer/compare/{{ .PreviousTag }}...{{ .Tag }}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - repository:
      owner: Alejandro-M-P
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    name: git-courer
    homepage: "https://github.com/Alejandro-M-P/git-courer"
    description: "AI-powered local git operations via MCP. Zero cloud tokens for git."
    license: "MIT"
    commit_msg_template: "chore: update git-courer formula to {{ .Tag }}"
    test: |
      system "#{bin}/git-courer --version"
