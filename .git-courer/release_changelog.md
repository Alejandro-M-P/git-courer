## Installer
- The self-update process could fail if asset names didn't match specific conventions — now the installer supports dual naming to ensure seamless updates

## Cli 
-Fixed a bug in the release command where custom tag messages (--message) completely overwrote the AI-generated changelog without warning.
