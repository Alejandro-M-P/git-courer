# Release changelog artifact (generated at runtime)
release_changelog.md

# Binaries
git-courer
*.exe
*.dll
*.so
*.dylib
*.a
!ffi/windows_amd64/libts_pack_core_ffi.a

# Rust build output (keep pre-built FFI lib for CI)
target/
!target/release/
target/release/*
!target/release/*/
!target/release/*/libts_pack_core_ffi.a
!target/release/*/libts_pack_core_ffi.so
!target/release/libts_pack_core_ffi.a
!target/release/libts_pack_core_ffi.so

# Test binary
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/
node_modules/

# Node deps (plugin)
package.json
package-lock.json

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local development
.env
.env.local
*.pem
*.key
test/test_credentials.go
temp_hold/

# Opencode config (local)
.opencode/

# MCP configs (local)
.claude/
.cursor/

# git-courer config (project-specific)

# SDD artifacts (local)
sdd/

# Runtime data

# Internal docs (not for git)
SPEC.md

CLAUDE.md
.atl/
AGENTS.md
.code-intel
workspace/
.agents/

# Roadmap local (not for git)

scripts/generate-languages-json/__pycache__/generate.cpython-314.pyc

Mejorar_commits_y_releases

# Antigravity CLI
.antigravitycli/
.tokensave

# Release audit artifacts
test/release/audit/

# CI release: downloaded native binaries for gh release upload
native-binaries/

